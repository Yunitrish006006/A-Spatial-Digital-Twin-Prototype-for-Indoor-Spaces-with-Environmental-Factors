[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "room-digital-twin"
version = "0.1.0"
description = "Single-room digital twin prototype for temperature, humidity, and illuminance fields."
requires-python = ">=3.9"
